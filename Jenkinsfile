pipeline {

    agent any

    stages {

        state("build") {
            echo 'building'
        }

        state("test") {
            echo 'testing'
        }

        state("deploy") {
            echo 'deploying'
        }

    }

}